services:
  app:
    container_name: sonatina
    image: sonatina
    build: .
    environment:
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
